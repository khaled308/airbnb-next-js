"use client";
import Modal from "@/components/modals/Modal";

export default function Home() {
  return (
    <main>
      <Modal
        title="khaled"
        isOpen
        onClose={() => {}}
        onSubmit={() => {}}
        secondaryAction={() => {}}
        secondaryActionLabel="Close"
      />
    </main>
  );
}
